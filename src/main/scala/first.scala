import first.a

object first extends App {

  val a = 1
  val b = 2

  def adder (a:Int,b:Int):Int = {
    val addValues = a+b
    addValues
  }


  def product (a:Int,b:Int):Int = {
    val multiplyValues = a*b
    multiplyValues
  }
}
